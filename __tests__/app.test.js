/* eslint-disable no-undef */

// describe("App-wide API tests", () => {

//   it("should get the list of products", async () => {
//     await getRequest(end.listProducts)
//       .expect("Content-Type", /json/)
//       .expect(200);
//   });
// });

test('Jest setup test', () => {
  expect(1).toBe(1);
})